#include <stdio.h>

int main() {
    // constants => fixed value that cannot be altered by the program during its execution
    // add 'const' at front of the keyword
    // make all the letter capital (it's not complusory but it's a naming convention)

    const float PI = 3.14159;

    printf("%f",PI);

    return 0;
}